# 接口测试

## 2026-07-24 批次 1-3 基础能力

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260723`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

执行结果：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| `transcribe_audio` | 未执行真实请求 | 需要用户在设置页填写 Mimo Key 并授权麦克风后实测；接口代码随 Tauri release 编译通过 |
| `process_text` | 编译通过 | 新增 `contextApp`、`styleInstruction` 字段；真实 AI 输出依赖 Mimo Key |
| `paste_text` | 编译通过 | 沿用 `pbcopy` + CoreGraphics；真实自动粘贴需要用户授予辅助功能权限后确认 |
| `register_shortcuts` | 编译通过 | 支持动态注册三种模式快捷键；重复快捷键会报错 |
| `get_runtime_diagnostics` | 编译通过 | 新增快捷键注册状态与失败原因字段，供首页和设置页展示真实诊断 |
| `set_login_launch` / `get_login_launch` | 编译通过 | 使用用户级 LaunchAgent，不需要管理员权限 |
| `set_dock_visible` | 编译通过 | 使用 Tauri `set_dock_visibility` |
| `get_frontmost_app` | 编译通过 | 使用 System Events 读取前台 App，真实结果受 macOS 权限影响 |
| `set_system_output_muted` | 编译通过 | 录音时静音开关默认关闭，开启后停止/取消会恢复之前状态 |

命令：

- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `cargo fmt --check --manifest-path src-tauri/Cargo.toml`：通过。
- `npx tauri build --bundles app`：通过。

## 2026-07-24 批次 22 快捷键注册诊断

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260723`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

执行结果：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| `register_shortcuts` | 编译通过 | 注册成功时写入 ready 状态；注册失败时保留失败原因，前端保存失败后可刷新诊断 |
| `get_runtime_diagnostics` | 编译通过 | 返回 `shortcutRegistrationReady` 和 `shortcutRegistrationMessage`，设置页可显示真实注册结果 |

命令：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo fmt --check --manifest-path src-tauri/Cargo.toml`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `npx tauri build --bundles app`：通过。

阻塞/未覆盖：

- 未执行真实 ASR/AI 请求，因为本轮不回显也不落盘用户 Mimo Key，需要用户在本机设置页输入后手动试录。
- 快捷键被系统占用的真实失败文案需要在用户本机冲突场景中确认。

## 2026-07-24 批次 23 Mimo 真实接口冒烟

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- Key 来源：macOS Keychain，未打印密钥。
- ASR 模型：`mimo-v2.5-asr`
- AI 模型：`mimo-v2.5`

执行结果：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| Mimo ASR | 通过 | HTTP `200`，耗时 `927ms`，模型 `mimo-v2.5-asr`，文本 `TypeSess测试语音输入。` |
| Mimo AI 文本 | 通过 | HTTP `200`，耗时 `2237ms`，模型 `mimo-v2.5`，文本 `TypeSess测试语音输入。` |

证据文件：

- `output/e2e/codexman-asr-smoke-result.json`
- `output/e2e/codexman-ai-smoke-result.json`

未覆盖：

- 本批没有通过 WebAudio 真实麦克风录音，也没有触发 `paste_text` 自动粘贴。

## 2026-07-24 批次 24 快捷键失败恢复与最终接口复核

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260723`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

执行结果：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| `register_shortcuts` | 通过 | 新快捷键注册失败时自动恢复上一组运行中快捷键，避免用户失去原快捷键入口 |
| `get_runtime_diagnostics` | 通过 | 返回真实快捷键注册状态和诊断文案，首页与设置页可展示失败原因 |
| `save_mimo_api_key` | 通过 | Key 继续写入 macOS Keychain，不落源码、配置、localStorage 或文档 |
| `transcribe_audio` | 真实接口冒烟通过 | Mimo ASR HTTP `200`，耗时 `927ms`，输出证据不含 Key |
| `process_text` | 真实接口冒烟通过 | Mimo AI HTTP `200`，耗时 `2237ms`，输出证据不含 Key |
| `paste_text` | 编译通过 | 自动粘贴仍依赖用户本机辅助功能授权和目标输入焦点 |

命令：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo fmt --check --manifest-path src-tauri/Cargo.toml`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `git diff --check`：通过。
- `npx tauri build --bundles app`：通过。

证据文件：

- `output/e2e/codexman-asr-smoke-result.json`
- `output/e2e/codexman-ai-smoke-result.json`

未覆盖：

- 未自动制造系统快捷键冲突场景；恢复逻辑已通过 Rust 编译、前端同步逻辑和桌面打包验证。
- 未主动触发用户当前输入框粘贴，避免污染真实输入内容。

## 2026-07-24 批次 25 空语音占位结果接口侧复核

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260723`

执行结果：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| ASR 空文本 | 通过 | 空字符串不进入 AI 总结、历史保存或自动粘贴 |
| ASR 占位文本 | 通过 | `无实际内容输出` 等占位文案会被识别为空语音 |
| 顶部错误提示 | 通过 | 空语音时提示用户靠近麦克风后重试 |

命令：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo fmt --check --manifest-path src-tauri/Cargo.toml`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `git diff --check`：通过。
- `npx tauri build --bundles app`：通过。

未覆盖：

- 真实 Mimo ASR 占位文案枚举可能随上游变化，后续如果出现新占位格式，需要补充词表。

## 2026-07-24 批次 33 AI 润色速度诊断与超时兜底

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260723`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

诊断结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 当前短文本 `process_text` | 通过 | 同一听写 prompt 连续 3 次真实请求耗时约 `986ms-1649ms`，平均约 `1.23s` |
| 带前台 App 上下文的短文本 | 通过 | 连续 10 次真实请求耗时约 `0.65s-1.3s` |
| 历史慢记录复测 | 通过 | 本地历史中 `7s-30s` 的慢记录原文逐条复测后耗时约 `0.5s-2.6s`，未稳定复现慢文本 |
| ASR 音频侧复测 | 通过 | `3.5s-10.7s` 的 16k/48k WAV 真实 ASR 耗时约 `0.79s-1.55s` |
| 本地配置检查 | 通过 | 词典仅 `1` 个词，个人风格为空，慢速不是由超长词典或超长偏好引起 |
| 口述润色超时兜底 | 通过 | `Dictate` 文本处理设置 `4.5s` 请求超时，超时后前端复用已有原始 ASR 兜底逻辑 |

本批改动：

- `process_text` 的 Mimo 文本处理请求按模式设置超时：口述 `4.5s`、翻译 `9s`、问答 `15s`。
- `transcribe_audio` 不设置该短超时，避免长音频 ASR 被误伤。
- 去掉前端 `styleInstruction` 中重复注入的前台 App 文案，前台 App 仍通过 `contextApp` 正常传给 Rust。

命令：

- `npm run lint`：通过。
- `npm run build`：通过。
- `PATH="$HOME/.rustup/toolchains/stable-aarch64-apple-darwin/bin:$PATH" cargo fmt --check`：通过。
- `PATH="$HOME/.rustup/toolchains/stable-aarch64-apple-darwin/bin:$PATH" cargo check`：通过。
- `git diff --check -- src-tauri/src/lib.rs src/main.ts`：通过。
- `PATH="$HOME/.rustup/toolchains/stable-aarch64-apple-darwin/bin:$PATH" npx tauri build --bundles app`：通过。

未覆盖：

- 未自动按物理 `Control + P` 触发真实麦克风录音和自动粘贴，避免污染用户当前输入框；本批以真实 Mimo 接口复测、本地历史数据和桌面包构建作为证据。

## 2026-07-26 口述 AI 润色动态超时回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 口述 AI 润色超时策略 | 通过 | 口述模式把本次 `recordElapsedMs` 传给 `process_text`，Rust 按 `4.5s + 录音时长 * 0.5` 计算等待时间，范围限制在 `4.5s-15s` |
| 短句口述回退 | 通过 | 短音频仍可快速回退 ASR 原文，避免 AI 润色拖慢自动粘贴 |
| 长段口述整理 | 通过 | 长音频获得更长 AI 润色窗口，不再固定压到 `4.5s` |
| 历史重新整理 | 通过 | 历史记录重试复用原始 `recordElapsedMs`，保持和真实口述一致 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-dictate-dynamic-timeout-hub-20260726.png`。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未自动触发真实物理 `Control + P` 录音和外部输入框自动粘贴，避免污染用户当前输入；本轮交给用户基于新包做真实口述体验确认。

## 2026-07-26 口述快捷键缺配置汇总提示回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 缺配置汇总 | 通过 | 口述录音前不再遇到第一个缺失项就返回，会汇总 Mimo Key、麦克风、快捷键、自动粘贴等缺失配置 |
| 快捷键触发提示 | 通过 | 缺配置时悬浮条显示 `口述配置未完成：请先设置...`，并打开 Hub 进入口述设置 |
| Hub 提示 | 通过 | Hub 顶部同步展示完整缺失清单，并打开第一个缺失项的权限说明弹窗 |
| Hub 按钮触发 | 通过 | 点击口述卡片的继续配置也使用同一套汇总提示 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-dictate-missing-permissions-summary-20260726.png`。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未模拟真实系统层面同时撤销 Mimo Key、麦克风权限和辅助功能权限后的物理 `Control + P`，避免改动用户本机权限；本轮由用户基于新包做真实快捷键体验确认。

## 2026-07-26 口述录音 Escape 退出回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| Escape 退出录音 | 通过 | 悬浮录音窗口监听 `keydown`，录音中按 `Escape` 会复用现有取消录音逻辑 |
| 跳过转写 | 通过 | Escape 退出会清空本次采样、停止音频流、恢复静音状态，不进入 ASR 转写流程 |
| 页面无展示入口 | 通过 | 未在页面新增可见快捷键说明，保持隐藏兜底能力 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-escape-cancel-recording-shell-20260726.png`。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未自动触发真实物理录音后按键盘 `Escape`，避免占用用户麦克风和污染当前输入；本轮交给用户基于新包做真实口述体验确认。

## 2026-07-26 口述开始录音准备态回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

问题判断：

- 快捷键触发后，`getUserMedia`、`AudioContext` 和采样节点创建需要一点时间；如果用户马上开口，前几个字可能发生在采样链路就绪之前。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 准备态 loading | 通过 | 开始录音请求进入后立即显示 `正在准备录音，出现录音状态后再开始说话。`，悬浮条进入 busy/loading 状态 |
| 录音态切换 | 通过 | 麦克风流、AudioContext 和采样节点连接完成，并执行 `audioContext.resume()` 后，才切换为真正录音态 |
| 防重复操作 | 通过 | 准备录音期间临时禁用悬浮条操作按钮，避免重复触发 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-recording-start-loading-20260726.png`。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未自动触发真实物理 `Control + P` 后马上说话的麦克风音频对比测试；本轮交给用户基于新包做真实口述体验确认。

## 2026-07-26 口述提示词泄漏防护回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

问题判断：

- Mimo 文本处理偶发没有只返回整理后的文本，而是复述了口述润色请求里的内部上下文，导致 `词典：`、`应用：`、`规则：`、`ASR：` 等内容进入自动粘贴结果。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| Prompt 收紧 | 通过 | 口述系统提示新增禁止输出或复述 `词典、应用、规则、ASR` 等内部标签 |
| 粘贴前防护 | 通过 | 前端在 `process_text` 返回后检查内部提示词结构，命中泄漏时只保留 `ASR：` 后面的真实转写文本 |
| 历史记录防护 | 通过 | 清洗发生在 `processRecognizedText` 返回阶段，保存历史和自动粘贴共用同一份清洗后的输出 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-prompt-leak-guard-hub-20260726.png`。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未强制调用真实 Mimo 复现提示词泄漏，因为该问题是模型偶发输出；本轮通过输出清洗逻辑和用户提供的泄漏样例做代码防护，交给用户基于新包做真实口述体验确认。

## 2026-07-26 口述提示词泄漏根因修复回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

根因结论：

- 旧口述请求把 `词典：`、`应用：`、`规则：`、`ASR：` 放在 user prompt 里，模型偶发把 user prompt 当成待整理内容复述，导致内部提示词进入最终粘贴结果。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| user prompt 降噪 | 通过 | 口述模式的 user prompt 改为只发送原始 ASR 文本，不再包含 `词典/应用/规则/ASR` 标签 |
| system prompt 承载规则 | 通过 | 口述整理规则、前台应用、词典和个人偏好均移动到 system prompt |
| 兜底清洗保留 | 通过 | 前端提示词泄漏清洗保留为最后保险，但不再作为主修复手段 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- 代码审计：`ProcessMode::Dictate => (build_dictate_system_prompt(request, &dictionary), text.to_string())`，确认口述 user prompt 仅为原始 ASR 文本。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-dictate-prompt-rootfix-hub-20260726.png`。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未强制伪造 Mimo 真实泄漏响应；本轮以请求结构根因修复和代码审计为主，交给用户基于新包做真实口述体验确认。

## 2026-07-26 口述完整链路风险门禁回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

风险结论：

- 子代审查确认口述链路的主要风险在阶段入口门禁不足：录音准备态、AI 整理输出、粘贴目标一致性和粘贴校验语义需要在进入下一阶段前阻断问题，而不是等结果异常后兜底。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 录音准备门禁 | 通过 | 配置缺失时不再先进入准备录音态；准备录音期间按 Escape 会取消，不进入录制或转写 |
| 麦克风启动顺序 | 通过 | 正式拿到麦克风流后才记录开始采集并执行系统静音，避免设备失败时产生误导状态 |
| AI 输出门禁 | 通过 | Rust 和前端双层检测内部提示词结构，命中后阻止进入结果、历史和粘贴流程 |
| AI 失败粘贴门禁 | 通过 | 口述 AI 润色开启时，AI 超时、空输出或异常只展示 ASR 原文供确认，不自动粘贴 |
| 粘贴目标一致性 | 通过 | 粘贴前要求当前前台 App 与录音触发时目标一致；不一致时不写剪贴板、不发 Cmd+V |
| 粘贴校验语义 | 通过 | 快速粘贴模式不再把未回读输入框正文标记为已验证写入 |
| 录音时长上限 | 通过 | 录音达到最长时长会自动停止并进入转写，避免内存和请求体失控 |
| 快捷键录制恢复 | 通过 | 快捷键录制增加超时取消，避免全局快捷键长期暂停 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-dictate-flow-risk-guards-20260726.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未在快捷键触发前增加真实 Key/API 探测缓存，避免把网络探测放进热路径导致快捷键启动变慢；无效 Key 仍会在 ASR/AI 请求阶段报错并保留诊断。
- 未自动触发真实物理快捷键、真实麦克风和外部 App 输入框组合链路；本轮交给用户基于新包做真实口述体验确认。

## 2026-07-26 静音口述不进入 ASR/AI 回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

问题判断：

- 用户点击快捷键开始录音但不说话，停止后仍进入 AI 润色，根因是空音频或近静音音频仍被送到 ASR，ASR 偶发返回 `原文` 等占位/幻觉文本；前端只要看到非空文本就继续进入 AI。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| ASR 前静音门禁 | 通过 | 停止录音后先分析 PCM 的 RMS、峰值和有效采样比例，低于阈值直接提示没有有效语音 |
| 阻止 AI 润色 | 通过 | 静音门禁失败时不调用 ASR，也不会进入 `processRecognizedText` |
| ASR 占位过滤 | 通过 | `原文`、`原文如下`、`原始文本`、`转写文本` 被列为空转写占位，返回后不会继续处理 |
| 诊断记录 | 通过 | 静音拦截会记录 RMS、峰值和有效采样比例，便于排查阈值是否过严 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-silent-recording-asr-guard-20260726.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未自动触发真实物理快捷键静音录音，避免占用用户麦克风；本轮交给用户基于新包做真实静音停止验证。

## 2026-07-26 口述短句语义限定词保留回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

问题判断：

- 用户口述“现在感觉已经没有问题了吧”，AI 润色后变成“没有问题了吧”，丢失了“现在、感觉、已经”等时间状态和主观判断限定词，语义发生收缩。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 口述规则收紧 | 通过 | 明确“我感觉、我觉得、现在、已经、可能、应该、吧、吗”等不是无意义语气词 |
| 短句保真 | 通过 | 短句或明确表态只做标点和错字修正，不允许删减主观、时间、程度、推测和语气限定 |
| 样例约束 | 通过 | 增加“现在感觉已经没有问题了吧”不能改成“没有问题了吧”的明确示例 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-dictate-preserve-qualifiers-20260726.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未调用真实 Mimo 对该口述样例做在线回归；本轮通过系统提示根因约束修复，交给用户基于新包做真实口述体验确认。

## 2026-07-26 口述整体链路最终审计回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

审计结论：

- 口述链路已按“阶段入口门禁”重新审计：配置缺失、录音准备、静音录音、ASR 空内容、AI 提示词泄漏、AI 润色失败、粘贴目标漂移和输入焦点缺失都不会继续进入后续阶段。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 粘贴焦点前置门禁 | 通过 | 现在先检测可输入焦点，再写临时剪贴板；没有输入框时不碰剪贴板、不发 Cmd+V |
| 剪贴板保护 | 通过 | 目标漂移、无辅助功能、无输入焦点、剪贴板备份失败都会在写入前停止 |
| 口述主链路审计 | 通过 | 录音、ASR、AI 和粘贴阶段均具备失败即停止的硬门禁 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，12 passed，1 ignored。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-dictate-final-flow-audit-20260726.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未用真实外部输入框逐项模拟全部系统权限和焦点异常组合；本轮以代码审计、静态检查、Rust 单测和页面验证作为交付前确认。

## 2026-07-26 托盘口述历史菜单回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 托盘菜单入口 | 通过 | 在系统托盘菜单中新增 `口述历史记录` 子菜单 |
| 最近口述历史 | 通过 | 前端启动、保存、删除和清空历史时同步最近 8 条口述历史给 Rust 原生菜单 |
| 点击复制 | 通过 | 点击子菜单某条历史后，Rust 直接把完整输出写入系统剪切板 |
| 复制提示 | 通过 | 复制成功后显示 `已复制到剪切板` 顶部提示 |
| 空历史态 | 通过 | 无口述历史时子菜单展示禁用项 `暂无口述历史记录` |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，12 passed，1 ignored。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-tray-dictation-history-menu-20260726.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- Playwright 无法直接操作 macOS 原生托盘右键/悬浮子菜单；原生菜单能力通过 `cargo check`、`cargo test` 和 Tauri 打包验证，交给用户在新包中做真实托盘交互确认。

## 2026-07-26 口述规则说明型提示词泄漏拦截回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

问题现象：

- 用户真实口述后，AI 输出了 `通用的规则描述如下`、编号规则列表、`删除无意义语气词`、`保留主观和限定词` 等内部整理规则，而不是整理后的口述文本。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 系统提示前置约束 | 通过 | 口述系统提示明确禁止输出规则描述、编号规则列表、处理原则、操作说明或任何元信息 |
| Rust 输出门禁 | 通过 | 原生侧识别规则说明标题和多个规则正文标记，命中后阻断 AI 输出进入后续流程 |
| 前端输出门禁 | 通过 | 页面侧同步识别同类规则说明输出，避免写入历史、剪切板或输入框 |
| 失败处理 | 通过 | 命中内部规则泄漏时停止使用 AI 文本，防止错误内容继续自动粘贴 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，12 passed，1 ignored。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-rule-description-leak-guard-20260726.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 该问题来自 Mimo 偶发生成错误，本轮无法稳定强制在线复现；本次通过系统提示约束和前后端输出门禁阻断，交给用户基于新包做真实口述体验确认。

## 2026-07-26 口述历史音频播放回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 历史音频保存 | 通过 | 录音转写成功后把本次 WAV 写入 Tauri app data 的 `dictation-audio` 目录，历史记录只保存文件路径和大小 |
| 历史播放器 | 通过 | Hub 历史记录中有音频的条目展示原生 `audio controls` 播放器，可点击播放当时录音 |
| 旧历史兼容 | 通过 | 旧历史和选中文本润色历史没有音频路径时不展示播放器，不影响复制、重试和删除 |
| 存储清理 | 通过 | 删除单条历史会清理不再被引用的音频文件，清空历史会同步清空历史音频目录 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，12 passed，1 ignored。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-history-audio-player-20260726.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- Playwright 通过模拟 Tauri 本地资源协议验证播放器 UI；真实录音文件播放需要在签名后的桌面包中由用户做一次实际口述后点击历史播放器确认。

## 2026-07-26 口述录音快速启动回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

问题判断：

- 快捷键触发后的冷启动链路包含配置检查、麦克风流获取、AudioContext 创建、采样节点连接、系统静音和提示音等步骤；如果用户立即开口，前几个字可能发生在采样节点真正开始回调之前。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 采样前置 | 通过 | 拿到麦克风流后立即设置录音态、记录开始时间并连接 WebAudio 采样节点 |
| 非关键动作后移 | 通过 | 系统静音、诊断日志和提示音移到采样启动之后，不再阻塞首帧采集 |
| 首帧等待缩短 | 通过 | 录音采样缓冲从 4096 降到 1024，并使用 `latencyHint: "interactive"` |
| 静音竞态保护 | 通过 | 系统静音异步完成时校验录音 token，结束后才返回的静音结果会立即恢复原状态 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-recording-fast-start-20260726.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未自动做真实快捷键后立即发声的声学对比测试；该项需要用户基于新包实际口述确认前几个字是否仍丢失。

## 2026-07-26 ChatGPT 粘贴前焦点偶发丢失回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

问题判断：

- 转写完成后自动粘贴前，Rust 只读取一次 macOS `AXFocusedUIElement`；ChatGPT / Electron / Web 输入框偶发在窗口状态切换或处理返回后短暂返回无焦点，导致系统误判“当前没有聚焦可输入区域”并打开结果窗口。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 焦点短重试 | 通过 | 粘贴前焦点检测由单次读取改为 4 次短重试，每次间隔 75ms，总窗口约 300ms |
| 安全边界保留 | 通过 | 仍然要求最终检测到可输入焦点后才写临时剪贴板和发送 Cmd+V，不放宽到无焦点强行粘贴 |
| 诊断可见 | 通过 | 焦点短暂丢失后恢复时，会在焦点摘要中记录重试恢复信息 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，12 passed，1 ignored。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-paste-focus-retry-20260726.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未自动复现 ChatGPT 输入框 AX 焦点偶发丢失；该问题依赖真实 ChatGPT 窗口和系统焦点时序，需要用户基于新包继续实测。

## 2026-07-26 Hub 历史记录分页回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

问题判断：

- Hub 历史记录进入页面时一次性渲染全部历史卡片；口述历史增加音频信息后，历史数量较多时 DOM 创建和控件初始化会造成明显卡顿。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 分页渲染 | 通过 | 历史记录每页只渲染 20 条，避免一次性创建大量卡片和音频控件 |
| 筛选联动 | 通过 | 切换全部、口述、翻译、随便问、润色筛选时自动回到第一页 |
| 翻页操作 | 通过 | 底部分页展示当前范围、总数、当前页和总页数，支持上一页/下一页 |
| 删除兼容 | 通过 | 删除历史后页码会自动夹到有效页，不会停留在空白页 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- Playwright 页面验证：通过，预置 45 条历史后确认第一页只渲染 20 条、下一页统计正确；截图 `output/playwright/codexman-history-pagination-20260726.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未用用户真实历史库做耗时采样；本轮通过大量模拟历史验证分页渲染行为，交给用户基于真实历史进入 Hub 体验卡顿是否消失。

## 2026-07-26 Hub 历史录音播放器 Error 回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

问题判断：

- 历史录音播放器使用 `convertFileSrc` 直接把本地文件路径交给 `<audio>`；当前 Tauri 配置未启用 asset 本地资源协议，导致 audio 控件加载失败并显示 `Error`。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 播放路径 | 通过 | 不再依赖 asset 本地资源协议，改为点击播放时通过 Rust 命令读取历史 WAV |
| 懒加载 | 通过 | 历史卡片初始只展示播放按钮，点击后才读取文件并创建 Blob URL，避免进入历史页时加载大量音频 |
| 错误提示 | 通过 | 文件不存在或读取失败时在卡片内展示明确错误，不再只显示浏览器原生 `Error` |
| 内存清理 | 通过 | 删除或清空历史时释放已创建的 Blob URL |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，12 passed，1 ignored。
- Playwright 页面验证：通过，模拟 Tauri 读取 WAV 后点击播放，截图 `output/playwright/codexman-history-audio-lazy-play-20260726.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未直接播放用户截图中的那条真实音频文件；本轮通过同路径类型的懒加载播放器验证，交给用户基于新包点击真实历史录音确认。

## 2026-07-26 转写完成后外部输入框失焦回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

问题判断：

- 外部输入框口述结束后，CodexMan 的主悬浮窗仍以置顶窗口状态停留在屏幕上，直到 `paste_text` 内部完成焦点检测后才隐藏；macOS 下置顶 WebView 即使设置为非聚焦窗口，也可能让 ChatGPT 这类 Web 输入框的 `AXFocusedUIElement` 在转写等待期间变空，最终被系统判断为“当前没有聚焦可输入区域”。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 隐藏时机前移 | 通过 | 外部输入框录音停止并通过有效语音门禁后，开始 ASR 前立即隐藏悬浮窗 |
| 粘贴前窗口清理 | 通过 | `paste_text` 开始时先隐藏 main/result 临时窗口，再等待 90ms 后读取前台 App 和输入焦点 |
| 根因约束 | 通过 | 不放宽无焦点粘贴规则；修复点是避免 CodexMan 自己的置顶窗口参与焦点判断 |
| Hub 场景兼容 | 通过 | 从 Hub 发起并保持 Hub 显示的录音仍不走自动粘贴，不受本次外部输入框修复影响 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，12 passed，1 ignored。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-hide-transient-before-paste-20260726.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未自动复现真实 ChatGPT 输入框 AX 焦点失焦时序；该问题依赖 macOS 当前前台窗口和真实输入框状态，需要用户基于新包做口述粘贴实测。

## 2026-07-26 停止录音快捷键重复展示悬浮窗回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

问题判断：

- 第二次按口述快捷键停止录音时，Rust 仍然走 `trigger_voice_mode -> present_window("main")`，会在停止录音前重新 show 一次置顶悬浮窗，并延迟 180ms 后才通知前端停止录音；这次窗口状态变化发生得比 ASR/AI 更早，是外部输入框失焦的更前置根因。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 停止路径不再 present | 通过 | 主悬浮窗已可见时，快捷键只发送停止事件，不再重新 show / present 主窗口 |
| 停止路径取消延迟 | 通过 | 主悬浮窗已可见时，不再等待 180ms，直接通知前端停止录音 |
| 启动路径兼容 | 通过 | 主悬浮窗不可见时仍按原逻辑展示悬浮窗并等待 WebView 就绪 |
| 根因约束 | 通过 | 修复的是结束快捷键制造的窗口状态变化，不通过放宽无焦点粘贴规则解决 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，12 passed，1 ignored。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-stop-shortcut-no-represent-20260726.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未自动驱动真实 ChatGPT 输入框执行完整快捷键录音和粘贴；该项需要用户基于新包做真实链路复测。

## 2026-07-26 AI 润色处理中悬浮窗提前消失回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

问题判断：

- 上一轮为了避免外部输入框被 CodexMan 置顶悬浮窗影响，在 `stopRecordingAndTranscribe` 通过有效语音门禁后、进入 ASR/AI 前就调用了 `hideFloatingWindow()`。
- 这会导致用户刚停止录音，悬浮窗在“转写中 / AI 润色中”阶段提前消失；此时结果尚未产出，也没有进入最终粘贴流程，隐藏时机过早。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 处理态显示 | 通过 | 停止录音后不再在 ASR/AI 前隐藏悬浮窗，保留转写中、AI 润色中和完成状态 |
| 粘贴前隐藏 | 通过 | 自动粘贴仍由原生 `paste_text` 在读取焦点前隐藏 main/result 临时窗口 |
| 根因约束 | 通过 | 修复隐藏时机错误，不放宽无焦点粘贴规则，也不增加“无焦点强粘贴”兜底 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，12 passed，1 ignored。
- 源码断言验证：通过，`stopRecordingAndTranscribe` 调用 `transcribeAudio` 前不再包含 `hideFloatingWindow()`。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-dictate-keep-floating-processing-20260726.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未自动驱动真实 ChatGPT 输入框执行完整快捷键录音、AI 润色和粘贴；该项需要用户基于新包做真实链路复测。

## 2026-07-26 Web 输入框 AX 焦点误判回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

问题判断：

- 自测发现浏览器/Electron 页面中 textarea 已经是 DOM active 状态时，macOS `AXFocusedUIElement` 仍可能返回 `NO_FOCUS`。
- 因此之前要求粘贴前必须读到 `AXTextArea` / `AXTextField` 会误判 ChatGPT 这类 Web 输入框没有焦点，导致结果已经生成但错误弹出“当前没有聚焦可输入区域”。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| AX 误判修正 | 通过 | 如果录音开始时有明确外部目标 App，且粘贴前目标 App 未变化，即使 AX 未暴露 DOM 输入焦点，也继续发送系统粘贴 |
| 目标变化保护 | 通过 | 目标 App 与录音开始时不一致时仍阻止粘贴，不写剪贴板、不发送 Cmd+V |
| 输入焦点快照 | 通过 | 开始录音时尽量记录 AX 文本输入控件坐标；粘贴前 App 已恢复但焦点仍为空时，会先尝试恢复该输入区域 |
| 剪贴板保护 | 通过 | 所有目标 App 校验和焦点门禁都发生在写临时剪贴板之前 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，16 passed，1 ignored。
- 原生 AX 自测：通过，已复现 Web textarea active 但 `AXFocusedUIElement` 返回 `NO_FOCUS` 的系统行为。
- 源码断言验证：通过，显式目标 App 信任门禁发生在写剪贴板之前。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-web-focus-explicit-target-paste-20260726.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未自动驱动真实 ChatGPT 输入框执行完整快捷键录音、AI 润色和粘贴；该项需要用户基于新包做真实链路复测。

## 2026-07-26 粘贴前恢复目标 App 输入焦点回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

问题判断：

- 用户真实复测仍出现“当前没有聚焦可输入区域”，说明结果已经生成，但粘贴前 `AXFocusedUIElement` 为空。
- 悬浮窗在 ASR/AI 处理中保持可见后，ChatGPT 这类 Web 输入框可能在等待期间丢失输入焦点；仅在 `paste_text` 开始时隐藏悬浮窗，不足以让外部输入框自动恢复焦点。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 目标 App 恢复 | 通过 | 只有录音开始时记录了明确外部 App，且粘贴前仍解析为同一个目标 App，才通过 System Events 恢复该 App 前台 |
| 粘贴门禁不放宽 | 通过 | 恢复目标 App 后仍必须重新读取到可输入焦点，失败时不写剪贴板、不发送 Cmd+V |
| 写剪贴板前复核 | 通过 | 读取焦点通过后、写入临时剪贴板前，再次确认当前前台 App 仍是原目标 App |
| 切换 App 保护 | 通过 | 如果用户在处理期间切到别的 App，仍会阻止自动粘贴，避免写错位置 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，13 passed，1 ignored。
- 源码断言验证：通过，目标 App 恢复和输入焦点检查都发生在写剪贴板之前。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-paste-refocus-before-clipboard-20260726.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未自动驱动真实 ChatGPT 输入框执行完整快捷键录音、AI 润色和粘贴；该项需要用户基于新包做真实链路复测。

## 2026-07-27 显式目标粘贴热路径提速回归

环境：

- 项目路径：`/Users/lucifer/Documents/source/t/monorepo/aiTool`
- 分支：`dev-20260726`
- 桌面包：`src-tauri/target/release/bundle/macos/CodexMan.app`

问题判断：

- 用户反馈 AI 生成完成后状态栏先消失，但还要等待约 1 到 2 秒才粘贴到外部输入框。
- 代码排查确认 `paste_text` 在结果已生成后仍串行执行多段等待：隐藏窗口等待、目标 App 激活等待、输入区域恢复等待、AX 焦点 4 次轮询、剪贴板恢复等待和诊断等待。
- 对 ChatGPT / 浏览器 / Electron Web 输入框而言，录音开始时已有明确外部目标 App，且粘贴前目标 App 未变化时，继续等待 AX 文本焦点并不增加确定性，反而会制造明显延迟。

修复结论：

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 显式目标快速路径 | 通过 | 录音开始目标 App 与粘贴前前台 App 一致时，不再执行 App 激活、输入区域恢复和 AX 文本焦点轮询 |
| 目标变化保护 | 通过 | 写临时剪贴板前仍复核当前前台 App 必须等于录音开始目标 App，变化时阻止粘贴 |
| 固定等待收敛 | 通过 | 生成完成到用户可见粘贴前的固定等待约束在 80ms 内，命令返回前固定等待约束在 140ms 内 |
| 非显式目标门禁 | 通过 | 没有明确录音目标 App 的粘贴仍保留严格 AX 可输入焦点检测，不扩大误粘范围 |

验证证据：

- `npm run lint`：通过。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过，执行时临时把 `/Users/lucifer/.rustup/toolchains/stable-aarch64-apple-darwin/bin` 加入 PATH。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，17 passed，1 ignored。
- Playwright 页面验证：通过，截图 `output/playwright/codexman-fast-explicit-target-paste-20260727.png`。
- `npx tauri build --bundles app`：通过，已使用证书 `Apple Development: shuo Li (KCW9LKVGJ6)` 签名。
- 新包重启：通过，当前运行路径 `/Users/lucifer/Documents/source/t/monorepo/aiTool/src-tauri/target/release/bundle/macos/CodexMan.app`。

未覆盖：

- 未自动驱动真实 ChatGPT 输入框执行完整快捷键录音、AI 润色和粘贴，避免污染用户当前输入内容；该项需要用户基于新包做真实链路复测。
