# 实时字幕模式技术文档

## 文档状态

| 字段 | 内容 |
| --- | --- |
| 版本 | 草案 v0.1 |
| 日期 | 2026-07-25 |
| 目标项目 | typesass / AI Talk 桌面端 |
| 功能类型 | 新增模式 |
| 默认快捷键 | `Control + Shift + S` |
| 风险等级 | L2 |

## 需求确认

实时字幕是一个独立于现有口述、翻译、随便问之外的新模式。用户按下快捷键后，应用进入“字幕监听模式”，开始采集音频输入并持续进行 ASR 识别。只要 ASR 返回可展示字幕，屏幕底部就显示实时字幕条；同时右上角展示可拖动的字幕历史列表。

第一版默认快捷键暂定为 `Control + Shift + S`，原因是现有口述模式已经使用 `Control + P`，翻译模式使用 `Control + T`，随便问模式使用 `Control + Space`，实时字幕需要避免和现有语音输入流程冲突。后续可纳入快捷键设置页统一配置。

## 功能目标

1. 用户可通过快捷键快速进入或退出字幕监听模式。
2. 字幕监听模式开启后，应用开始采集音频输入。
3. 音频输入目标包括麦克风声音和电脑系统播放声音；正式方案需要支持两路音频同时采集、对齐和混音后送入 ASR。
4. ASR 返回字幕后，屏幕底部出现半透明黑色字幕条。
5. 底部字幕条平时隐藏，有内容时才显示。
6. 底部字幕条宽度和高度随文字长度自动变化，类似视频播放器或会议字幕，不使用固定大面板。
7. 屏幕右上角展示半透明黑色字幕历史记录窗口。
8. 历史记录窗口可以拖动位置，并展示已经固化的字幕记录。

## 非目标范围

第一版不做以下能力：

1. 不做账号、云同步、多人协作或会议房间。
2. 不做说话人分离的强承诺；如保留麦克风和系统音频双轨，可为后续区分“我方/对方”预留数据基础。
3. 不做逐词级字幕效果承诺；第一版以句子或短片段级字幕为目标。
4. 不默认保存完整原始音频；如需要保存，必须单独增加用户开关和隐私说明。
5. 不把历史窗口做成完整笔记编辑器；第一版只展示、滚动、清空或复制。

## 交互流程

| 步骤 | 用户行为或系统状态 | 期望结果 |
| --- | --- | --- |
| 1 | 用户按下 `Control + Shift + S` | 进入字幕监听模式，开始采集音频 |
| 2 | 监听中采集到有效声音 | 本地按时间片段送入 ASR |
| 3 | ASR 返回临时字幕 | 屏幕底部显示实时字幕条 |
| 4 | 文字变长或换行 | 字幕条宽度和高度跟随内容自适应变化 |
| 5 | 当前句子被固化 | 当前字幕追加到右上角历史记录 |
| 6 | 一段时间没有新字幕 | 底部字幕条自动淡出或隐藏，历史记录保留 |
| 7 | 用户拖动历史窗口 | 历史窗口移动到新位置，并可持久化位置 |
| 8 | 用户再次按快捷键 | 退出字幕监听模式，停止音频采集和 ASR 请求 |

## 字幕监听模式

字幕监听模式是一个全局模式，不依赖 Hub 主窗口是否打开。进入后应保持后台运行，并通过独立悬浮窗口展示字幕结果。

### 模式状态

| 状态 | 含义 | UI 行为 |
| --- | --- | --- |
| `idle` | 未监听 | 底部字幕条隐藏；历史窗口可隐藏或保留上次状态 |
| `listening` | 正在采集音频 | 可显示轻量状态标识；底部字幕条等待 ASR 内容 |
| `transcribing` | 正在请求 ASR | 底部字幕条可继续展示上一段临时字幕 |
| `displaying` | 正在展示字幕 | 底部字幕条显示最新字幕 |
| `error` | 音频权限或 ASR 异常 | 展示轻量错误提示，不写入历史 |

### 快捷键规则

1. 默认快捷键为 `Control + Shift + S`。
2. 首次按下：进入字幕监听模式。
3. 监听中再次按下：退出字幕监听模式。
4. 如果快捷键注册失败，需要沿用现有快捷键诊断机制，在 Hub 中提示冲突原因并引导用户修改。
5. 后续实现时，实时字幕快捷键应进入统一快捷键配置表，和口述、翻译、随便问共用保存与注册逻辑。

## 底部实时字幕条

底部实时字幕条用于展示“当前正在说的话”或最近一次 ASR 返回的临时字幕。它不是固定面板，而是随文字内容动态变化的全局悬浮字幕。

### 展示规则

1. 默认隐藏。
2. ASR 返回非空字幕时显示。
3. 背景为半透明黑色。
4. 文本使用高对比白色，保证在浅色和深色桌面背景上都能阅读。
5. 字幕条水平居中，显示在屏幕底部安全区域上方。
6. 宽度随文本长度增长，但需要设置最大宽度，避免横跨整个屏幕。
7. 超过最大宽度后自动换行，高度随行数增加。
8. 建议最多展示 2 到 3 行，超出部分由新字幕替换或在历史窗口查看。
9. 一段时间没有新字幕时自动淡出或隐藏。

### 窗口行为

1. 使用透明、无边框、置顶窗口承载。
2. 字幕条本身不需要用户交互，建议设置为不抢焦点。
3. 字幕条不应遮挡用户当前输入焦点或打断其他应用操作。
4. 多显示器场景下，默认展示在当前活跃屏幕底部；若无法判断，则展示在主屏幕底部。

## 右上角字幕历史窗口

字幕历史窗口用于展示已经固化的字幕记录，不展示每一次会被修正的临时字幕。

### 展示内容

| 字段 | 说明 |
| --- | --- |
| 时间 | 字幕固化时间，可用短时间格式展示 |
| 文本 | 已固化字幕正文 |
| 来源 | 可选字段，后续可标记麦克风、系统音频或混合音频 |
| 状态 | 可选字段，标识普通记录、识别失败或被清空 |

### 交互规则

1. 历史窗口默认出现在屏幕右上角。
2. 背景为半透明黑色。
3. 窗口可拖动。
4. 拖动后记录窗口位置，下次进入字幕监听模式时恢复。
5. 历史列表支持滚动。
6. 第一版建议提供清空和复制入口；如时间不够，可先只做列表展示和拖动。
7. 历史窗口需要可交互，因此不能整体设置为完全点击穿透。

## 音频采集要求

实时字幕模式需要采集麦克风声音和电脑系统播放声音。由于 macOS 默认 WebAudio 只能稳定采集麦克风，系统声音需要通过原生层或虚拟音频设备实现。2026-07-25 已验证 ScreenCaptureKit 在当前机器上只返回画面帧、不返回系统音频帧；第一版系统音频方案改为 macOS 14.4+ Core Audio Process Tap，并保留虚拟声卡作为兜底方案。

### 第一版建议

1. 麦克风采集复用现有 WebAudio 录音能力。
2. 系统音频采集优先使用 Core Audio Process Tap 原生层能力；如果目标系统不支持或权限失败，再提示用户配置虚拟声卡方案。
3. 内部保留麦克风轨道和系统音频轨道，完成时间戳对齐后再混音送入 ASR。
4. 即使第一版最终只混音给 ASR，也不要在数据结构上完全丢失来源字段，避免后续无法扩展说话人或来源标记。

### 系统音频验证方式

第一版测试系统音频采集时，可由用户在电脑里持续播放一段带人声的音乐、访谈或视频音频，然后开启字幕监听模式，观察底部实时字幕条是否出现字幕。该测试的第一判定目标是“系统播放声音能不能触发字幕出现”，不要求歌词或台词在冒烟测试阶段完全逐字准确。

该验证用于确认“电脑系统播放声音 -> 系统音频采集 -> ASR -> 字幕悬浮层展示”的链路是否打通。测试音源应优先选择有清晰人声或歌词的内容；如果播放的是纯伴奏、环境音或无人声音乐，ASR 可能不会返回可展示文字，此时不能直接判定字幕功能失败。

### 权限要求

| 权限 | 用途 | 说明 |
| --- | --- | --- |
| 麦克风 | 采集用户说话声音 | 现有录音流程已经需要 |
| 系统音频捕获权限 | 采集电脑系统播放声音 | Core Audio Process Tap 依赖 `NSAudioCaptureUsageDescription` 对应的系统音频捕获授权 |
| 辅助功能 | 不一定是字幕必需 | 若仅展示字幕，不自动粘贴，则可不强依赖辅助功能 |

### 语音模式权限展示规则

转写、翻译、随便问、实时字幕的权限状态属于具体语音模式，不再作为首页或全局设置里的统一前置条件展示。每个语音模式卡片和模式详情页都需要展示本模式依赖的权限项，权限项包含名称、当前状态、勾或叉图标、状态说明和“去设置/查看”入口。

用户点击任一权限项时，应用弹出权限说明弹窗，说明该权限为什么被当前模式需要、应该如何开启，以及一个主按钮跳转到对应设置位置。麦克风权限跳转 macOS 麦克风隐私设置，辅助功能权限跳转 macOS 辅助功能设置，API Key、快捷键和系统声音来源跳转到应用内对应配置控件。用户通过快捷键或模式启动按钮进入某个语音模式时，如果缺少该模式必需权限，应用需要给出“请设置 XXX 权限”的提示，并自动打开 Hub 的对应模式详情和权限说明弹窗。

| 模式 | 权限项 | 缺失时行为 |
| --- | --- | --- |
| 口述 | API Key、麦克风、辅助功能、快捷键 | 阻止开始录音，提示设置缺失权限并进入口述详情 |
| 翻译 | API Key、麦克风、辅助功能、快捷键 | 阻止开始录音，提示设置缺失权限并进入翻译详情 |
| 随便问 | API Key、麦克风、快捷键 | 阻止开始录音，提示设置缺失权限并进入随便问详情 |
| 实时字幕 | API Key、麦克风、系统声音来源、快捷键 | 阻止字幕监听或提示配置系统声音来源，并进入实时字幕详情；默认系统声音来源为“原生系统音频捕获” |

## ASR 调用策略

实时字幕不能只等用户停止说话后再调用 ASR。第一版应采用“固定时间切片 + 停顿固化”的策略。

1. 监听中持续采集 PCM 音频。
2. 每隔固定时间切出一个音频片段送入 ASR，例如 `1.2s - 2s`。
3. 每个片段保留 `300ms - 500ms` 重叠区，降低切断词语导致的识别错误。
4. ASR 返回后先作为底部临时字幕展示。
5. 检测到明显停顿后，将当前句子固化并写入历史窗口。
6. 如果用户长时间连续说话没有明显停顿，需要设置强制切段上限，例如 `6s - 10s`，避免一直不返回字幕。
7. 固化字幕时需要做相邻片段去重，避免重叠音频导致重复文字。

## 数据流

```text
快捷键开启字幕监听
-> 麦克风音频采集
-> 系统音频采集
-> 双轨时间戳对齐
-> 混音或分轨缓存
-> 固定时间切片
-> 调用 Mimo ASR
-> 返回临时字幕
-> 底部字幕条展示
-> 停顿或强制切段后固化
-> 写入右上角历史窗口
```

## 验收标准

| 编号 | 验收项 | 通过标准 |
| --- | --- | --- |
| RTS-001 | 快捷键进入模式 | 按 `Control + Shift + S` 后进入字幕监听模式 |
| RTS-002 | 快捷键退出模式 | 监听中再次按快捷键后停止采集并停止发送 ASR 请求 |
| RTS-003 | 底部字幕隐藏 | 没有字幕内容时，底部字幕条不可见 |
| RTS-004 | 底部字幕显示 | ASR 返回字幕后，屏幕底部出现半透明黑色字幕条 |
| RTS-005 | 字幕自适应尺寸 | 文本较短时字幕条较窄；文本较长时自动扩宽或换行增高 |
| RTS-006 | 非固定大面板 | 底部字幕条不以固定大窗口展示，不长期占用屏幕底部大面积空间 |
| RTS-007 | 历史窗口展示 | 固化字幕后，右上角历史窗口追加记录 |
| RTS-008 | 历史窗口拖动 | 用户可以拖动历史窗口，拖动后位置生效 |
| RTS-009 | 连续说话可出字 | 用户没有明显停顿时，也能通过固定时间切片持续返回字幕 |
| RTS-010 | 短暂停顿不中断 | 用户短暂停顿时不错误退出监听，也不导致字幕链路卡死 |
| RTS-011 | 音频权限异常 | 麦克风或系统音频权限不足时，展示明确提示，不静默失败 |
| RTS-012 | 不抢焦点 | 字幕展示不打断用户在其他应用中的当前操作 |
| RTS-013 | 系统音频字幕验证 | 用户持续播放带人声的电脑系统音频时，开启字幕监听模式后底部字幕条能展示 ASR 返回的字幕 |

## 待澄清问题

1. 历史窗口默认是否一直显示，还是只在有历史记录后显示。
2. 历史窗口第一版是否需要“复制全部”和“清空”按钮。
3. 底部字幕在无新内容后自动隐藏的时间，建议默认 `3s - 5s`。
4. 是否需要为麦克风和系统声音分别展示来源，还是第一版只展示混合字幕。
5. 系统音频采集第一版是否接受虚拟声卡方案作为过渡。
6. 多显示器环境下，字幕跟随鼠标所在屏幕、前台应用所在屏幕，还是固定主屏幕。

## 实现影响范围预估

| 范围 | 说明 |
| --- | --- |
| Rust / Tauri | 新增实时字幕快捷键、悬浮窗口、Core Audio Process Tap helper、系统音频采集命令、窗口拖动和置顶控制 |
| 前端 TypeScript | 新增字幕状态机、字幕条渲染、历史列表渲染、ASR 切片调度 |
| 音频处理 | 新增固定切片、重叠、静音检测、强制切段、混音和去重 |
| 本地存储 | 新增历史窗口位置、字幕历史、实时字幕设置 |
| 设置页 | 后续增加实时字幕快捷键、字幕显示、历史窗口开关等配置 |
| 测试验证 | 需要真实桌面、真实麦克风、系统音频和 Mimo ASR 联调 |

## 开发自测记录

| 字段 | 内容 |
| --- | --- |
| 自测时间 | 2026-07-26 |
| 风险等级 | L2 |
| 判断依据 | 新增实时字幕核心流程，涉及快捷键、音频采集、ASR 调用、悬浮窗口、历史记录和本地配置 |
| 闭环状态 | 代码、构建、桌面打包、权限提示、页面视觉验证和真实系统播放声音连续出字幕已闭环 |
| 覆盖范围 | `Control + Shift + S` 快捷键配置、字幕窗口显示/隐藏、历史窗口展示、固定时间切片、原生系统音频 8 秒切片、静音固化、强制固化、系统音频设备选择、异常提示、模式级权限卡片和详情权限 |
| 测试环境 | macOS 桌面端；App 包路径：`src-tauri/target/release/bundle/macos/typesass.app`；音源：macOS 音乐 App 播放中文歌曲；ASR：`mimo-v2.5-asr`；Key 来源：macOS Keychain；系统音频来源：Core Audio Process Tap |
| 静态检查 | `npm run lint` 通过；`npm run build` 通过；`cargo fmt --check --manifest-path src-tauri/Cargo.toml` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过 |
| 桌面打包 | `npx tauri build --bundles app` 通过，产物：`src-tauri/target/release/bundle/macos/typesass.app` |
| 页面验证 | 使用本地预览 `http://127.0.0.1:1421` 验证字幕窗口和历史窗口，截图：`output/playwright/typesass-subtitle-hidden-20260725.png`、`output/playwright/typesass-subtitle-short-20260725.png`、`output/playwright/typesass-subtitle-long-wrap-20260725.png`、`output/playwright/typesass-subtitle-history-20260725.png` |
| 权限验证 | 使用本地预览和最新 Tauri 包验证语音模式卡片权限、权限弹窗、实时字幕详情权限和权限缺失启动拦截；实时字幕缺少明确系统声音来源时，快捷键会进入 Hub 并提示“请设置实时字幕的系统声音来源。”；截图：`output/playwright/typesass-mode-permissions-20260725.png`、`output/playwright/typesass-permission-dialog-system-audio-20260725.png`、`output/playwright/typesass-subtitle-permission-detail-20260725.png`、`output/e2e/typesass-subtitle-shortcut-system-audio-required-20260725.png` |
| 真实音乐验证 | 2026-07-25 使用 macOS 音乐 App 播放中文歌曲验证；MediaRecorder 直传 WebM 会被 Mimo 拒绝为 `Param Incorrect`，改为 WAV 转码后曾成功返回 1 条字幕。22:29 继续验证：ScreenCaptureKit 采集音乐 App 与系统 `say` 播报时均为 `audioBuffers=0`；Oray 虚拟设备没有产出回环输入样本。22:43 切换 Core Audio Process Tap：`.app` demo 成功采集 Apple Music，`typesass-process-tap` 正式包 helper 成功采集系统音频 WAV，Mimo ASR HTTP `200`。2026-07-26 继续验证时发现旧版 `active` 使用全局系统音频，会混入 Chrome、Typeless 和 typesass WebKit 进程声音，导致字幕像随机输出；已改为自动选择单个活跃音频进程并排除自身/GPU/helper 噪声。最终打包版验证中，helper 摘要稳定为 `target=音乐 pid=91592`；前端实时字幕 UI 连续以 8 秒系统音频切片发送 ASR，`aiToolDiagnosticLogV1` 显示连续返回字幕并写入 `aiToolSubtitleHistoryV1`，字幕内容连续命中当前播放歌曲的副歌和后续段落 |
| 真实证据路径 | 单进程音乐采集 WAV：`output/e2e/typesass-auto-target-music-8s-20260726.wav`；ASR 结果：`output/e2e/typesass-auto-target-music-8s-asr-zh-20260726.json`；最终打包版底部字幕悬浮窗单窗口截图：`output/e2e/typesass-live-subtitle-final-window-20260726.png`；最终打包版本地数据断言：`output/e2e/typesass-live-subtitle-final-localstorage-20260726.json`；诊断日志包含 `target=音乐 pid=91592`、`片段时长：8000ms`、`ASR 已返回可展示字幕` |
| API/数据断言 | 字幕历史写入 `aiToolSubtitleHistoryV1`，遵循现有历史保留策略；ASR 仍复用 `transcribe_audio` 和 Mimo `mimo-v2.5-asr` |
| Console/Network/日志 | 前端构建无类型错误；Rust 编译无错误；Tauri 打包无阻塞错误；macOS 统一日志可见 `typesass-process-tap` 采集和 WebView ASR 请求 |
| 用例汇总 | 17 条：通过 13 条，条件通过 4 条，失败 0 条，阻塞 0 条，跳过 0 条 |
| 未覆盖项 | 未做多语种歌曲、视频会议、浏览器视频和嘈杂环境全量回归；歌曲字幕仍受 ASR 模型与音源清晰度影响；双轨同时混音、虚拟声卡兜底完整回归作为后续增强项 |
| 手动 SQL 清单 | 无 |

## 测试矩阵

| 用例 ID | 关联验收项 | 风险等级 | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 验证方式 | 结果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| RTS-TC-001 | RTS-001、RTS-002 | L2 | P0 | Tauri 桌面端启动，快捷键注册成功 | 按 `Control + Shift + S` 开启，再次按下退出 | 字幕监听模式可进入和退出，退出后停止采集和 ASR 请求 | `cargo check`、Tauri 打包、快捷键注册代码审计；macOS 合成按键不作为物理快捷键最终证据 | 条件通过，真实物理快捷键需用户在桌面最终确认 |
| RTS-TC-002 | RTS-003、RTS-004、RTS-006 | L2 | P0 | 本地预览可访问 | 打开 `?mode=subtitle`，检查无字幕和有字幕两种状态 | 无字幕时隐藏；有字幕时显示半透明黑色字幕条，非固定大面板 | Playwright 截图与 DOM 尺寸检查 | 通过 |
| RTS-TC-003 | RTS-005 | L2 | P1 | 本地预览可访问 | 分别注入短字幕和长字幕 | 短字幕宽度较窄；长字幕达到最大宽度后换行增高 | Playwright DOM 尺寸检查和截图 | 通过 |
| RTS-TC-004 | RTS-007、RTS-008 | L2 | P1 | 本地预览可访问，本地字幕历史有数据 | 打开 `?mode=subtitleHistory`，写入两条固化字幕 | 历史窗口展示记录，复制和清空按钮可用；Tauri 真实窗口可拖动 | Playwright DOM 检查、Tauri 窗口配置审计 | 通过，拖动手感需桌面最终确认 |
| RTS-TC-005 | RTS-009、RTS-010 | L2 | P0 | 麦克风权限和 Mimo Key 可用 | 持续说话超过普通停顿周期，再短暂停顿继续说话 | 固定时间切片持续发送 ASR；短暂停顿不退出监听；静音或强制上限才固化 | 代码审计、构建检查 | 条件通过，真实音频需用户最终确认 |
| RTS-TC-006 | RTS-011 | L2 | P0 | 麦克风权限未授权或系统音频设备不可用 | 开启字幕监听 | 展示明确错误；系统音频不可用时可降级麦克风并写诊断日志 | 代码审计、构建检查 | 通过 |
| RTS-TC-007 | RTS-012 | L2 | P1 | Tauri 桌面端启动 | 展示字幕窗口时继续操作其他应用 | 字幕底部窗口不抢焦点，历史窗口可交互 | Tauri 配置审计 | 通过，真实桌面焦点需用户最终确认 |
| RTS-TC-008 | RTS-013 | L2 | P0 | 播放带人声音源，系统声音来源为原生系统音频捕获 | 开启字幕监听并播放系统声音 | 系统音频能进入原生采集链路并触发 ASR 字幕 | Core Audio Process Tap demo、正式包 helper、真实 Mimo ASR 请求、Tauri 前端 UI 真实启动日志和本地字幕历史 | 通过：2026-07-26 前端 UI 连续采集 `target=音乐` 的 8 秒系统音频切片，ASR 连续返回字幕并写入历史 |
| RTS-TC-009 | RTS-011 | L2 | P0 | 打开 Hub 语音模式页 | 查看口述、翻译、随便问、实时字幕四个模式卡片 | 每个模式卡片展示自己的权限列表，权限项显示勾/叉状态和说明 | Playwright DOM 检查和截图 | 通过 |
| RTS-TC-010 | RTS-011 | L2 | P0 | 打开实时字幕权限项 | 点击系统声音来源权限 | 弹出权限说明，展示设置步骤，主按钮可跳到应用内系统声音来源配置 | Playwright DOM 检查和截图 | 通过 |
| RTS-TC-011 | RTS-011 | L2 | P0 | 打开模式详情页 | 查看实时字幕详情和其他语音模式详情 | 详情页显示本模式权限；缺失权限可点击弹窗说明并跳转设置 | Playwright DOM 检查和截图 | 通过 |
| RTS-TC-012 | RTS-011 | L2 | P0 | 打开 Hub 语音模式页 | 分别检查口述、翻译、随便问、实时字幕的快捷键权限文案 | 四个模式展示自己的快捷键，不复用口述快捷键；实时字幕显示 `Control + Shift + S` | Tauri 可访问性树读取 | 通过 |
| RTS-TC-013 | RTS-011 | L2 | P0 | 打开 Hub 语音模式页 | 分别检查四个模式卡片和详情页权限项 | 口述/翻译展示 API Key、麦克风、辅助功能、快捷键；随便问展示 API Key、麦克风、快捷键；实时字幕展示 API Key、麦克风、快捷键、系统声音 | Playwright DOM 检查、Tauri 可访问性树读取 | 通过 |
| RTS-TC-014 | RTS-011 | L2 | P1 | 打开权限项 | 点击 API Key、麦克风、辅助功能、快捷键、系统声音权限项 | 均出现权限说明弹窗；系统权限跳 macOS 设置，应用配置项跳应用内设置 | Playwright DOM 检查、代码审计 | 条件通过，已覆盖主要权限弹窗，macOS 设置跳转需用户实机确认 |
| RTS-TC-015 | RTS-011 | L2 | P1 | 快捷键注册失败或冲突 | 保存冲突快捷键或读取注册失败诊断 | Hub 展示失败原因，引导修改快捷键，不静默进入字幕监听 | 历史冲突截图、代码审计 | 条件通过，已有 `output/playwright/typesass-shortcut-conflict-live-20260724.png` 证据，本轮未重复污染当前快捷键配置 |
| RTS-TC-016 | RTS-013 | L2 | P1 | Core Audio Process Tap 支持且音乐播放中 | helper 使用 `active` 自动选择目标 | 自动排除 typesass 自身、WebKit GPU、helper 噪声，优先选择音乐进程 | `typesass-process-tap` 直接运行、WAV/ASR 结果 | 通过，证据：`output/e2e/typesass-auto-target-music-8s-20260726.wav`、`output/e2e/typesass-auto-target-music-8s-asr-zh-20260726.json` |
| RTS-TC-017 | RTS-013 | L2 | P1 | 原生系统音频不可用或用户选择虚拟声卡 | 使用虚拟声卡/系统输入兜底路径 | 可回退到配置的系统声音输入；找不到输入时显示权限/配置提示 | 代码审计、历史 Oray/ScreenCaptureKit 失败记录 | 条件通过，虚拟声卡完整回归未覆盖，作为后续增强项 |

## 验收结论

| 字段 | 内容 |
| --- | --- |
| 验收时间 | 2026-07-26 |
| 验收结论 | 条件通过：核心“音乐 App 系统音频 -> ASR -> 底部实时字幕 -> 历史记录”链路通过；物理快捷键进入/退出、macOS 设置跳转和虚拟声卡兜底完整回归需用户实机确认 |
| 通过项 | 代码实现覆盖快捷键配置、字幕监听状态机、麦克风与系统音频输入选择、MediaRecorder 独立小段录制、Core Audio Process Tap 单进程系统音频采集、WAV 转码、底部字幕条、右上角历史窗口、复制/清空、错误提示、模式级权限卡片、模式详情权限、权限说明弹窗、设置跳转和桌面窗口配置 |
| Rule 合规审计 | 本次未修改公共包；新增/修改方法和类型已保留中文注释；未发现 `any`；未新增无关抽象；未使用测试脚本替代真实页面验证 |
| 测试证据 | `npm run lint`、`npm run build`、`cargo fmt --check --manifest-path src-tauri/Cargo.toml`、`cargo check --manifest-path src-tauri/Cargo.toml`、`npx tauri build --bundles app` 均通过；页面截图已写入 `output/playwright/`；最终打包版真实音乐字幕单窗口证据：`output/e2e/typesass-live-subtitle-final-window-20260726.png`；最终打包版本地数据断言：`output/e2e/typesass-live-subtitle-final-localstorage-20260726.json`；音频/ASR 证据：`output/e2e/typesass-auto-target-music-8s-20260726.wav`、`output/e2e/typesass-auto-target-music-8s-asr-zh-20260726.json` |
| 遗留风险 | Core Audio Process Tap 依赖 macOS 系统音频捕获权限；歌曲实时字幕受伴奏、人声清晰度、间奏和 ASR 模型能力影响，可能出现空片段或少量错字；当前已避免全局混音导致的随机输出，并以 8 秒系统音频切片提升音乐场景稳定性 |
| 用户需执行操作 | 请在真实键盘上按一次 `Control + Shift + S` 确认实时字幕可进入/退出；若 macOS 弹出“捕获其他应用音频”权限，需要允许 typesass；如未弹窗但采集失败，再进入隐私设置检查系统音频捕获授权 |
| 上线建议 | 可作为第一版音乐/系统音频实时字幕交付；后续若要追求歌词级逐字准确，需要引入歌词源对齐或音乐专用 ASR/人声分离能力 |
