{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "transient",
  "description": "悬浮与结果窗口最小权限",
  "windows": ["main", "toast", "result"],
  "permissions": [
    "core:default",
    "core:window:allow-internal-toggle-maximize",
    "core:window:allow-start-dragging"
  ]
}
