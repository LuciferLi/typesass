{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认窗口权限",
  "windows": ["main", "hub", "toast", "result"],
  "permissions": ["core:default", "opener:default"]
}
