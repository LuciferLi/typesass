{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认窗口权限",
  "windows": ["main", "hub", "toast", "result", "subtitle", "subtitleHistory"],
  "permissions": [
    "core:default",
    "core:window:allow-internal-toggle-maximize",
    "core:window:allow-start-dragging",
    "opener:default"
  ]
}
